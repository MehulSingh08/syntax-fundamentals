# syntax-fundamentals
- [trees/](trees/) – Binary Tree creation from traversals
- [recursion/](recursion/) - All recursion codes
- [graphs/](graphs) - Syntaxes and graph basics

Syntaxes for a stack in java

Stack<Integer> stk = new Stack<>(); //Intialisation
stk.pop();
stk.push(i);
stk.peek();
stk.isEmpty();


Syntaxes for Queue in java

Queue<Integer> que = new LinkedList<>();
q.offer(i);

// can be used to just remove only even if you do not return anything it will work anyways
q.poll(); // removes the first element from the queue and returns it, 
q.peek(); // just returns the first element