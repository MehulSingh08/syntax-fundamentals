Avoid corgo cult programming, do not write the code that serves no real purpose.

Syntaxes for important collections are listed below:
Trees:
Swapping elements by use of arithmetic operators:
     int a = 5;
     int b = 10;
     a = a + b;
     b = a - b;
     a = a - b;
Lets understand it, as a = (a+b), b=>(a+b)-b = a, a => (a+b)-a;

A class is a blueprint of an object and deciedes upon the major 
and basic functionalities of the object.

Object is an instance of the class.
For ex Dog is a class while Tommy is an object of the dog class.

Mehul(object) is an instance to human(class).

For tree Traversal.
Root = Your house address → Starting point to find everything else
TreeNode = The actual house → Contains the structure and connections
int value = Just the house number → Not enough info to visit

You need the actual house (TreeNode) as the starting point, not just the house number (int value)!

Basics of writing any algorithm:
1. Do not try to build that algo from the scratch.
2. First write the base case then the edge cases, rest will work out with the flow easily.
3. Always do the dry run for the code so that you understand the algorithm inherently without mugging it up.

